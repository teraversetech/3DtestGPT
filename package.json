{
  "name": "fashion3d-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "Fashion3D monorepo containing web, mobile, server, and worker apps",
  "scripts": {
    "dev:web": "pnpm --filter web dev",
    "dev:mobile": "pnpm --filter mobile start",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "format": "pnpm -r format"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  }
}
